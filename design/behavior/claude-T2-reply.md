Wrote `docs/links.md`. It covers what each link type is, how to create them (`ln` vs `ln -s`), how to tell them apart with `ls -l`, when to use which, and the gotcha where saving through a symlink in some editors replaces it with a plain file.

Next: open `docs/links.md` and skim it.
